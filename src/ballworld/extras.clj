(ns ballworld.extras)


 ;; (def f (frame :title "my app"))
  ;;(.setSize f 400 300)
  ;;(def handler (fn [e] (alert "pressed key!")))
  ;;(listen f :key-pressed handler)
  ;;(show! f) 

  ;;(def panel (JPanel.))
  ;;(listen panel :key-pressed handler)

;; (defn button-handler
 ;;   [event]
  ;;  (JOptionPane/showMessageDialog nil,
   ;;   (str "<html>Hello from <b>Clojure</b>. Button "
    ;;   (.getActionCommand event) " clicked.")))

;;  (def my-button (JButton. "I am a button"))
  ;; (su/add-action-listener my-button button-handler)

  ;; (.add f panel)

  ;;(future (loop [] (prn (time-flowing?)) (Thread/sleep 250) (recur)))

  ;;(future (loop [] (if (time-flowing?)
   ;;                  (do (update! ball)
    ;;                     (prn (:pos @ball))))
     ;;              (Thread/sleep 500)
      ;;             (recur)))

  ;;(future (loop [] (switch-timeflow!) (Thread/sleep 5000) (recur)))
